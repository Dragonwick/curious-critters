<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' | ' : '' %>Admin · Curious Critters</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@700&family=Crimson+Pro:ital,wght@0,300;0,400;0,600;1,300;1,400&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body class="admin-body">
  <div class="admin-layout">
    <aside class="admin-sidebar">
      <div class="admin-logo">
        <a href="/">🐾</a>
        <span>Admin</span>
      </div>
      <nav class="admin-nav">
        <a href="/admin" class="<%= typeof active !== 'undefined' && active === 'dashboard' ? 'active' : '' %>">📊 Dashboard</a>
        <a href="/admin/products" class="<%= typeof active !== 'undefined' && active === 'products' ? 'active' : '' %>">🦕 Products</a>
        <a href="/admin/orders" class="<%= typeof active !== 'undefined' && active === 'orders' ? 'active' : '' %>">📦 Orders</a>
        <a href="/admin/users" class="<%= typeof active !== 'undefined' && active === 'users' ? 'active' : '' %>">👤 Users</a>
        <a href="/admin/discounts" class="<%= typeof active !== 'undefined' && active === 'discounts' ? 'active' : '' %>">🏷 Discounts</a>
        <hr>
        <a href="/">← Storefront</a>
        <a href="/auth/logout">Logout</a>
      </nav>
    </aside>
    <main class="admin-main">
